from setuptools import setup, find_packages

setup(name='dynamic-3d-gaussians',
      version='0.1',
      description='Dynamic 3D Gaussian Splatting and Touch',
      license='MIT',
      packages=find_packages(),
      zip_safe=False)